name: kitepay_app_2026
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies: # ONLY ONE OF THESE
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  flutter_html: ^3.0.0
  kitepay_sdk: # Ensure this is indented correctly under the first dependencies
    path: ./kitepay_sdk 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true